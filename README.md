# Admin Portal

This is the Admin portal repository. It includes a Django backend and frontend build tools.

## Getting Started

First, install Python dependencies:

```bash
pip install -r requirements.txt
```

If you need to install frontend dependencies:

```bash
yarn install
```

Run the development server:

```bash
python manage.py runserver
```
